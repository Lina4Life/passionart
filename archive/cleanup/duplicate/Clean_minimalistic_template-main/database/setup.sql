-- Database Setup Script
-- Run this to create the PassionArt database

-- Connect to PostgreSQL as superuser first, then run:
-- CREATE DATABASE passionart;
-- CREATE USER passionart_user WITH PASSWORD 'your_secure_password';
-- GRANT ALL PRIVILEGES ON DATABASE passionart TO passionart_user;

-- Then connect to the passionart database and run the schema.sql and seed.sql files

-- Connection string example for .env file:
-- DATABASE_URL=postgresql://passionart_user:your_secure_password@localhost:5432/passionart

-- For local development, you can also use:
-- DATABASE_URL=postgresql://postgres:your_postgres_password@localhost:5432/passionart
